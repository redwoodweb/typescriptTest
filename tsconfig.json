{
  "compilerOptions": {
    "target": "es6",
    // "module": "ES6",
    "outFile": "./dist/bundle.js",//module should is system or amd, make to bundle one file
    "paths": {
      "@/*": ["./src/*"]
    },
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": [ // src 내부 모든 파일 포함
    "src/**/*.ts"
  ],
  "exclude": [ // src 내부 img 폴더 및 아래 모든 파일 제외
    "src/img/**/*.ts"
  ]
}